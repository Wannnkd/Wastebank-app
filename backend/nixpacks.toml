[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader"]

[start]
cmd = "php artisan migrate --force --seed && php artisan config:cache && php artisan serve --host=0.0.0.0 --port=${PORT}"
